#!/bin/bash

# Print the given Arguments: bash repeat.sh

# for ((i = 0 ; i < $2 ; i++));
# do
#   echo $1
# done

# -----------------------------------------------------------

# Print only the folder:

echo $(ls -d */)
